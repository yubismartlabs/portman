[package]
name = "portman"
version = "0.1.0"
description = "A local server manager"
authors = ["PortMan"]
edition = "2021"

[lib]
name = "portman_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.39.6"
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
thiserror = "2"
